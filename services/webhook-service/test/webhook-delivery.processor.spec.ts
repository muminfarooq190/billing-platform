import { WebhookDeliveryProcessor } from '../src/jobs/webhook-delivery.processor';

describe('WebhookDeliveryProcessor', () => {
  it('is defined', () => {
    expect(WebhookDeliveryProcessor).toBeDefined();
  });
});
